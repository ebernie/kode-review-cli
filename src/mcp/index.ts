/**
 * MCP (Model Context Protocol) module for kode-review
 *
 * This module provides:
 * - Tool handlers for file reading and code indexer queries
 * - MCP server that can be spawned as a child process
 */

export * from './tools/index.js'
